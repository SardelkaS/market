apiVersion: v1
kind: PersistentVolume
metadata:
  name: auth-redis-volume
  labels:
    type: local
    app: auth-redis
spec:
  storageClassName: manual
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: /data/redis