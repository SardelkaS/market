apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: core-postgres-volume-claim
  labels:
    app: core-postgres
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi